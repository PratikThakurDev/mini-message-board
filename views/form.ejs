<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/form.css" />
</head>
<body>
  <main class="container" role="main" aria-label="New Message Form">
    <h1><%= title %></h1>
    <form method="POST" action="/new" novalidate>
      <label for="messageUser" class="visually-hidden">Your Name</label>
      <input id="messageUser" type="text" name="messageUser" placeholder="Your Name" required autofocus autocomplete="off" />

      <label for="messageText" class="visually-hidden">Your Message</label>
      <input id="messageText" type="text" name="messageText" placeholder="Your Message" required autocomplete="off" />

      <button type="submit">Send</button>
    </form>
    <a class="back-link" href="/">Back to messages</a>
  </main>

  <style>
    /* Visually hidden for accessibility */
    .visually-hidden {
      position: absolute !important;
      height: 1px; width: 1px;
      overflow: hidden;
      clip: rect(1px, 1px, 1px, 1px);
      white-space: nowrap;
    }
  </style>
</body>
</html>
